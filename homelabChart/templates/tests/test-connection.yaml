apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homelabChart.fullname" . }}-test-connection"
  labels:
    {{- include "homelabChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "homelabChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
