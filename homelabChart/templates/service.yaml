apiVersion: v1
kind: Service
metadata:
  name: {{ include "homelabChart.fullname" . }}
  labels:
    {{- include "homelabChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homelabChart.selectorLabels" . | nindent 4 }}
